# Secrets — never commit. Wallet material lives outside the repo.
.mnemonic
.mnemonic.*
.wallet-seed
*.seed
.env
.env.*
!.env.example

# Dependencies / build
node_modules/
dist/
build/
.turbo/
*.tsbuildinfo
coverage/

# Gateway registry (SQLite, one file per environment)
gateway/*.db
gateway/*.db-journal

# Compact build artifacts
managed/
*.zkir

# Test and run output
logs/
*.log
agent/.state/

# Encrypted private-state store. Holds contract private state — nonce seeds and
# receipt secrets — and the dev password lives in source, so treat it as plaintext.
midnight-level-db/
*.ldb

# OS / editor
.DS_Store
.idea/
*.swp
.vercel
